{% extends 'base.html.twig' %}

{% block body %}
    <nav>
        <a href={{ path('home') }}>Home</a>
        <a href={{ path('login') }}>Login</a>
    </nav>

    <h1>
        Bienvenu
    </h1>

    <p>Les derniers articles !</p>

    {% for post in posts%}
        <h2>{{ post.title }}</h2>
    {% endfor %}


{% endblock %}